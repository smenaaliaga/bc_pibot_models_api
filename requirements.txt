# Core
fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
pydantic>=2.0,<3
pydantic-settings>=2.0,<3

# ML – JointBERT + Router
# IMPORTANT: transformers MUST stay on 4.46.x — DeBERTa-v3 forward pass changed
# in transformers 5.x, breaking the fine-tuned CRF slot tagger (all-O output).
torch>=2.2,<3
transformers>=4.46,<4.47
huggingface-hub>=0.24,<1
safetensors>=0.4
protobuf>=4.25,<6
sentencepiece>=0.2,<1
pytorch-crf>=0.7,<1

# ML – Router (multitask encoder + torch heads)
sentence-transformers>=5.2,<6
peft>=0.18,<1  # LoRA support for SetFit models

# Observability
prometheus-fastapi-instrumentator>=7.0,<8
structlog>=24.0
python-json-logger>=2.0,<3

# HTTP client (healthcheck, tests)
httpx>=0.27
